import React, { Component } from "react";
import PackeryPoc from "../Components/packeryPoc";

class Packery extends Component {
  render() {
    return (
      <div className="packery-page col-lg-12">
        <PackeryPoc />
      </div>
    );
  }
}

export default Packery;
